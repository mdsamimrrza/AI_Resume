# Ignore large/sensitive files from Vercel deployments
.env
*.tar.gz
*.zip

# Local dev only files
node_modules

# Lib dist (libs are bundled into backend/dist by esbuild)
lib/*/dist
frontend/dist

# Don't upload attached assets (not needed at runtime)
attached_assets
