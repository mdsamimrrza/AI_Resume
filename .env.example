# MongoDB Atlas connection string (required)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/<dbname>?retryWrites=true&w=majority

# Backend server port (local dev only, not needed on Vercel)
PORT=5000

# Environment
NODE_ENV=production
